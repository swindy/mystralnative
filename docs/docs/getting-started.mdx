# Introduction

Mystral Native.js is a lightweight runtime that lets you run JavaScript/TypeScript games natively using WebGPU. Think of it as "Electron for games" but without Chromium - just your game code, a JavaScript engine, and native WebGPU rendering.

## Why Mystral Native.js?

- **No Browser Required**: Your game code runs against native WebGPU, Canvas, and Audio implementations — no Chromium, no Electron
- **Familiar APIs**: Use the same WebGPU, Canvas, Audio, and fetch APIs you'd use in a browser
- **Multiple JS Engines**: Choose V8 for full ES2024+ support, QuickJS for small binaries, or JSC on Apple platforms
- **Cross-Platform**: Build for macOS, Windows, Linux, iOS, and Android from one codebase
- **Easy Distribution**: Ship a single binary - no dependencies, no installers

## Quick Example

```javascript
// Your game code - standard Web APIs
const adapter = await navigator.gpu.requestAdapter();
const device = await adapter.requestDevice();
const context = canvas.getContext("webgpu");

// Load assets with fetch
const model = await fetch("file://./assets/model.glb");

// Standard animation loop
function render() {
    // Your rendering code here
    requestAnimationFrame(render);
}
render();
```

## Supported Platforms

| Platform | Minimum Version | JS Engines | WebGPU Backend |
|----------|----------------|------------|----------------|
| macOS (arm64/x64) | macOS 14 Sonoma | V8, JSC, QuickJS | Dawn, wgpu-native |
| Windows | Windows 11 | V8, QuickJS | Dawn, wgpu-native |
| Linux | Ubuntu 24.04 / glibc 2.39 | V8, QuickJS | Dawn, wgpu-native |
| iOS | iOS 17 | JSC, QuickJS | wgpu-native |
| Android | Android 13 | V8, QuickJS | wgpu-native |

> **Note:** On Linux, the prebuilt binaries require glibc 2.39 or later (Ubuntu 24.04+). Older distributions may encounter `GLIBC_2.xx not found` errors. You can [build from source](/mystralnative/docs/guides/building) to target older glibc versions.

## Next Steps

- [Installation](/mystralnative/docs/installation) - Download or build Mystral Native.js
- [Quick Start](/mystralnative/docs/quick-start) - Run your first game
- [Running Games](/mystralnative/docs/guides/running-games) - Learn the CLI commands
